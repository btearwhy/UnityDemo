using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;



public class AssetDatabaseTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
/*        Effect_Duration_Dot_Data so = AssetDatabase.LoadAssetAtPath<Effect_Duration_Dot_Data>("Assets/Demo/Data/Effect_Duration_Dot_Data.asset");
        if (so == null)
        {
            // Create and save ScriptableObject because it doesn't exist yet
            so = ScriptableObject.CreateInstance<Effect_Duration_Dot_Data>();
            AssetDatabase.CreateAsset(so, "Assets/Demo/Data/Effect_Duration_Dot_Data.asset");
        }*/
    }
        
    // Update is called once per frame
    void Update()
    {
        
    }
}
