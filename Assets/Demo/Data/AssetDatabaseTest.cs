using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;



public class AssetDatabaseTest : MonoBehaviour
{
    // Start is called before the first frame update
  /*  void Start()
    {
        AbsorbAbility map = AssetDatabase.LoadAssetAtPath<AbsorbAbility>("Assets/Demo/Data/AbsorbAbility.asset");
        if (map == null)
        {
            // Create and save ScriptableObject because it doesn't exist yet
            map = ScriptableObject.CreateInstance<AbsorbAbility>();
            AssetDatabase.CreateAsset(map, "Assets/Demo/Data/AbsorbAbility.asset");
        }
    }*/

    // Update is called once per frame
    void Update()
    {

    }
}
